selmag:
  services:
    catalogue:
      uri: http://localhost:8081
      registration-id: keycloak

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/catalogue
    username: root
    password: root

  security:
    oauth2:
      client:
        provider:
          keycloak:
            issuer-uri: http://localhost:8082/realms/selmag
            user-name-attribute: preferred_username
        registration:
          keycloak:
            client-id: manager-app
            client-secret: 3UXcm0nuqNkL5oPOiOlw1vc80NeOkcM9
            scope:
              - openid
              - view_catalogue
              - edit_catalogue
              - microprofile-jwt
